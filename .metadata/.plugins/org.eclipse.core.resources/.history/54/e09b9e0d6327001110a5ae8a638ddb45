package in.cg.main.service;

import java.util.List;

import org.springframework.web.multipart.MultipartFile;

import in.cg.main.dto.PrescriptionUploadResponse;
import in.cg.main.entities.Prescription;

public interface PrescriptionService {

    // ✅ Use username from JWT instead of userId
    PrescriptionUploadResponse uploadPrescription(String username, MultipartFile file);

    // ✅ Fetch prescriptions for logged-in user
    List<Prescription> getByUsername(String username);

    // ✅ Admin functionality
    List<Prescription> getPendingPrescription();

    // ✅ Admin review
    Prescription reviewPrescription(Long id, boolean approved, String rejectionReason);
}