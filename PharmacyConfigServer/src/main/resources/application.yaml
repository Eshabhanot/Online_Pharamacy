server:
  port: 8888

spring:
  application:
    name: PharmacyConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Eshabhanot/Pharmacy_Microservices.git
          clone-on-start: true

management:
  endpoint:
    web:
      exposure:
        include: "*"